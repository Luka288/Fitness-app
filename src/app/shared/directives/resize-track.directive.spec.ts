import { ResizeTrackDirective } from './resize-track.directive';

describe('ResizeTrackDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeTrackDirective();
    expect(directive).toBeTruthy();
  });
});
