import { TransformStrPipe } from './transform-str.pipe';

describe('TransformStrPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformStrPipe();
    expect(pipe).toBeTruthy();
  });
});
